package com.martinsaman;

public class Main {
    /*
    Diferencia entre Iterator y Spliterator
        Iterator se introdujo en jdk 1.2, mientras que Spliterator se introdujo en jdk 1.8

        Iterator se usa para la API de colección, mientras que Spliterator se usa para la API de Stream

        Iterator se puede usar para iterar los elementos en Collection en orden secuencial,
        mientras que Spliterator se puede usar para iterar los elementos Stream en orden paralelo o secuencial.

     */
    /*
    Diferencias entre la API Date antigua de Java y la API de Date and Time de Java 8
        La API Date antigua no era segura para subprocesos,
        mientras que la API de Date and Time de Java 8 es segura para subprocesos.

        La API Date antigua era mutable, mientras que la API de Date and Time de Java 8 es inmutable.

        La API Date antigua define los meses del 0 al 11,
        mientras que la API de Date and Time de Java 8 define los meses del 1 al 12.
     */
}
